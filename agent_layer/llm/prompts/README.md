# Prompt boundary

Only structured failure summaries and normalized denied events may be sent to an
optional provider. Policies are generated and validated deterministically; the
provider never decides whether verification passed.

