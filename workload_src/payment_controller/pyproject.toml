[project]
name = "kuber-payment-controller"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "kubernetes>=30.1.0",
  "uvicorn>=0.30.0",
]

[dependency-groups]
dev = ["pytest>=8.3.0"]

